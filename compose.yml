services:
  sm-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sm-dashboard
    restart: unless-stopped
    env_file:
      - .env
    command: npm run server:start
    ports:
      - "3001:3001"
